<div class="invoice_header" style="border-bottom: 4px #CCCCCC double; padding-bottom: 10px;">
  <h4>OwlReply LLC.</h4>
  <p>[Business Address]</p>
  <br />
  <table>
    <tr>
      <td><strong>INVOICE NUMBER:</strong></td>
      <td><%= @invoice.number %></td>
    </tr>
    <tr>
      <td><strong>BILL TO:</strong></td>
      <td><%= @user.email_address %></td>
    </tr>
    <tr>
      <td><strong>DATE:</strong></td>
      <td><%= @invoice.date.strftime("%m/%d/%Y") %></td>
    </tr>
  </table>
</div>
<div class="invoice_body">
  <table class="table">
    <thead>
    <th>Description</th>
    <th class="text-right" width="10%">Amount</th>
    </thead>
    <tbody>
    <td><%= @invoice.subscription.name %> - Monthly Charge</td>
    <td class="text-right"><%= @invoice.amount_to_string %></td>
    </tbody>
  </table>
</div>
<div class="invoice_footer">
  <% if @invoice.amount_paid %>
    <small class="font-italic">Paid <%= @invoice.amount_paid_to_string %> on <%= @invoice.date_paid %></small>
  <% end %>
</div>