<div class="row">
  <div class="col-md-1"></div>
  <div class="col-md-10 add-reply">
    <%= link_to raw('<i class="icon-plus-sign"></i><span>Add New Automated Reply</span>'), new_emailaccount_reply_path(@emailaccount), class: "button button-rounded button-reveal button-small button-dirtygreen", id: "addnew_reply", remote: true %>
  </div>
  <div class="col-md-1"></div>
</div>

<div id="replies_container">
<% @replies.each do |reply| %>
  <%= render reply %>
<% end %>
</div>

<!--<p id="notice"><%#= notice %></p>

<table>
  <thead>
  <tr>
    <th>Keywords</th>
    <th>Body</th>
    <th>Negative keywords</th>
    <th>Catchcall</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody class="replies">
  <%# @replies.each do |reply| %>
    <%#= render reply %>
  <%# end %>
  </tbody>
</table>

<div id="new_record">
  <%#= link_to 'New Email Reply', new_emailaccount_reply_path(@emailaccount), remote: true %>
  <div class="record-form">
  </div>
</div>-->