<% @replies.each do |reply| %>
  <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-10 bottommargin-sm">
      <div class="white-section custom-white-box nopadding">
        <div class="card">
          <div class="card-header">

          </div>
          <div class="card-body">
            <%= form_with model: (reply || reply = Reply.new), html:{class: 'nobottommargin'} do |f| %>
              <% if reply.errors.any? %>
                <div class="style-msg2 errormsg">
                  <div class="msgtitle">Opps! There's an error upon submitting.</div>
                  <div class="sb-msg">
                    <ul>
                      <% reply.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                      <% end %>
                    </ul>
                  </div>
                </div>
              <% end %>
              <div class="form-group">
                <label>Keywords:</label>
                <%= f.text_field :keywords, class: 'form-control', readonly: 'readonly' %>
              </div>
              <div class="form-group">
                <label>Body</label>
                <div>
                  <%= f.text_area :body, class: 'form-control', readonly: 'readonly' %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<!--<p id="notice"><%#= notice %></p>

<table>
  <thead>
  <tr>
    <th>Keywords</th>
    <th>Body</th>
    <th>Negative keywords</th>
    <th>Catchcall</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody class="replies">
  <%# @replies.each do |reply| %>
    <%#= render reply %>
  <%# end %>
  </tbody>
</table>

<div id="new_record">
  <%#= link_to 'New Email Reply', new_emailaccount_reply_path(@emailaccount), remote: true %>
  <div class="record-form">
  </div>
</div>-->