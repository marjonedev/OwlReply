<%= form_with model: (ignoredword || ignoredword = Ignoredword.new), remote: true do |form| %>

    <% if ignoredword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ignoredword.errors.count, "error") %> prohibited this ignoredword from being saved:</h2>

      <ul>
      <% ignoredword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :word %>
    <%= form.text_field :word %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
